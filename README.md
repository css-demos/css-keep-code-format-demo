CSS Keep Code Format Demo
=========================

How to keep the content of some tag(e.g. `div`, `code`) with format unchanged.

The key is `white-space: pre;`.

Open `index.html` in your browser.